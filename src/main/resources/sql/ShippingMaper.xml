<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vt.cms.model.repository.ShippingRepository">
    <resultMap id="ShippingResultMap" type="com.vt.cms.model.entity.Shipping">
        <id column="id" property="id" jdbcType="OTHER"/>
        <result column="service_code" property="servicecode" jdbcType="VARCHAR"/>
        <result column="service_name" property="servicename" jdbcType="VARCHAR"/>
        <result column="fee" property="fee" jdbcType="OTHER"/>
    </resultMap>

    <select id="getShipping" resultMap="ShippingResultMap">
        SELECT *
        FROM shipping
    </select>
    
    <select id="detailShipping" resultMap="ShippingResultMap">
        SELECT *
        FROM shipping
        where service_code = #{servicecode}
    </select>


</mapper>